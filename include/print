#pragma once

#ifdef __INTELLISENSE__

#include <cstdio>

namespace std {
    template <class... Args>
    void print(const char*, Args&&...);

    template <class... Args>
    void println(const char*, Args&&...);

    void println();

    template <class... Args>
    void print(FILE*, const char*, Args&&...);

    template <class... Args>
    void println(FILE*, const char*, Args&&...);

    void println(FILE*);
}

#else

#include_next <print>

#endif
